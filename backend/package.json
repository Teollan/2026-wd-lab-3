{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "prisma generate && tsc",
    "start": "node --env-file-if-exists=.env dist/server.js",
    "dev": "prisma generate && tsc && node --env-file-if-exists=.env dist/server.js",
    "prisma:migrate": "prisma migrate dev",
    "prisma:prepare": "prisma migrate dev --create-only",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "fastify": "^5.8.5"
  },
  "devDependencies": {
    "@types/node": "^24.12.4",
    "prisma": "^7.8.0",
    "typescript": "^6.0.3"
  }
}
